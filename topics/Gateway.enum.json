{
  "JSON Response": [
    {
      "field": "url",
      "type": "string",
      "description": "The WSS URL that can be used for connecting to the gateway"
    },
    {
      "field": "shards",
      "type": "integer",
      "description": "The recommended number of shards to use when connecting"
    },
    {
      "field": "session_start_limit",
      "type": "session_start_limit object",
      "description": "Information on the current session start limit"
    }
  ]
}